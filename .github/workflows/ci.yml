name: Continuous Integration — Build & Publish

on:
  push:
    branches: [master, dev]
  pull_request:
    branches: [master, dev]

permissions:
  actions: read
  contents: read

jobs:
  # ========== JOB 1: Tests ==========
  test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run linter
        run: npm run lint || echo "⚠️ No lint configured"

      - name: Run tests
        run: npm test || echo "⚠️ No tests configured"

  # ========== JOB 2: Build PROD (master → app.formarte.co) ==========
  build-prod:
    name: Build PROD Docker Image (app.formarte.co)
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref_name == 'master'

    env:
      PROJECT_NAME: app.formarte.co
      DOCKER_IMAGE_NAME: app-formarte-co

    outputs:
      artifact-name: ${{ steps.vars.outputs.ARTIFACT_NAME }}
      git-sha: ${{ steps.vars.outputs.GIT_SHA_SHORT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate build variables
        id: vars
        run: |
          GIT_SHA_SHORT=$(echo ${{ github.sha }} | cut -c1-7)
          TIMESTAMP=$(date +%Y%m%d-%H%M%S)
          BRANCH_CLEAN=$(echo ${{ github.ref_name }} | tr '/' '-')
          ARTIFACT_NAME="${{ env.DOCKER_IMAGE_NAME }}-${BRANCH_CLEAN}-sha-${GIT_SHA_SHORT}-${TIMESTAMP}"
          echo "GIT_SHA_SHORT=${GIT_SHA_SHORT}" >> $GITHUB_OUTPUT
          echo "TIMESTAMP=${TIMESTAMP}" >> $GITHUB_OUTPUT
          echo "ARTIFACT_NAME=${ARTIFACT_NAME}" >> $GITHUB_OUTPUT
          echo "📦 Artifact: ${ARTIFACT_NAME}.tar.gz"

      - name: Build Docker image
        run: |
          docker build \
            --tag ${{ env.DOCKER_IMAGE_NAME }}:${{ steps.vars.outputs.GIT_SHA_SHORT }} \
            --tag ${{ env.DOCKER_IMAGE_NAME }}:latest \
            --label "git.commit=${{ github.sha }}" \
            --label "git.branch=${{ github.ref_name }}" \
            --label "build.timestamp=${{ steps.vars.outputs.TIMESTAMP }}" \
            --label "build.actor=${{ github.actor }}" \
            .

      - name: Save & compress Docker image
        run: |
          docker save ${{ env.DOCKER_IMAGE_NAME }}:${{ steps.vars.outputs.GIT_SHA_SHORT }} \
            -o ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar
          echo "📊 Raw size: $(du -sh ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar | cut -f1)"
          gzip -9 ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar
          echo "📊 Compressed: $(du -sh ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz | cut -f1)"

      - name: Install Python dependencies
        run: pip install requests PyJWT cryptography

      - name: Upload artifact to SharePoint
        env:
          ARTIFACT_FILE: ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CERT_THUMBPRINT: ${{ secrets.AZURE_CERT_THUMBPRINT }}
          AZURE_PRIVATE_KEY: ${{ secrets.AZURE_PRIVATE_KEY }}
          SHAREPOINT_SITE_URL: ${{ secrets.SHAREPOINT_SITE_URL }}
          SHAREPOINT_FOLDER: ${{ secrets.SHAREPOINT_FOLDER }}
        run: |
          cat > /tmp/upload_sp.py << 'EOFPY'
          import os, sys, time, uuid, hashlib, requests, jwt, base64, binascii
          from cryptography.hazmat.primitives import serialization
          from cryptography.hazmat.backends import default_backend

          TENANT_ID      = os.environ['AZURE_TENANT_ID']
          CLIENT_ID      = os.environ['AZURE_CLIENT_ID']
          THUMBPRINT     = os.environ['AZURE_CERT_THUMBPRINT']
          PK_PEM         = os.environ['AZURE_PRIVATE_KEY']
          SITE_URL       = os.environ['SHAREPOINT_SITE_URL'].rstrip('/')
          FOLDER_SUFFIX  = f"{os.environ['SHAREPOINT_FOLDER']}/{os.environ.get('PROJECT_NAME','')}"
          ARTIFACT_FILE  = os.environ['ARTIFACT_FILE']
          SITE_HOST      = SITE_URL.split('/')[2]
          SITE_PATH      = '/' + '/'.join(SITE_URL.split('/')[3:])
          DOC_LIB        = 'Documentos compartidos'
          CHUNK_SIZE     = 10 * 1024 * 1024

          def normalize_pem(s):
              return '\n'.join(l.strip() for l in s.strip().splitlines())

          def get_token():
              now = int(time.time())
              payload = {'aud': f'https://login.microsoftonline.com/{TENANT_ID}/oauth2/v2.0/token',
                         'exp': now+600, 'iss': CLIENT_ID,
                         'jti': hashlib.sha256(os.urandom(32)).hexdigest(),
                         'nbf': now, 'sub': CLIENT_ID}
              tb  = binascii.unhexlify(THUMBPRINT.replace(':','').replace(' ',''))
              x5t = base64.urlsafe_b64encode(tb).rstrip(b'=').decode('ascii')
              pk  = serialization.load_pem_private_key(normalize_pem(PK_PEM).encode(), password=None, backend=default_backend())
              ca  = jwt.encode(payload, pk, algorithm='RS256', headers={'x5t': x5t})
              r   = requests.post(f'https://login.microsoftonline.com/{TENANT_ID}/oauth2/v2.0/token',
                                  data={'client_id': CLIENT_ID,
                                        'client_assertion_type': 'urn:ietf:params:oauth:client-assertion-type:jwt-bearer',
                                        'client_assertion': ca,
                                        'scope': f'https://{SITE_HOST}/.default',
                                        'grant_type': 'client_credentials'}, timeout=30)
              if r.status_code != 200: print(f"❌ Token [{r.status_code}]: {r.text[:200]}"); sys.exit(1)
              return r.json()['access_token']

          def ensure_folder(token):
              from urllib.parse import quote
              api = f"https://{SITE_HOST}{SITE_PATH}/_api/web"
              h   = {'Authorization': f'Bearer {token}', 'Accept': 'application/json;odata=nometadata'}
              sp  = os.environ['SHAREPOINT_FOLDER']; pn = os.environ.get('PROJECT_NAME','')
              par = f"{SITE_PATH}/{DOC_LIB}/{sp}"; tgt = f"{par}/{pn}"
              if requests.get(f"{api}/GetFolderByServerRelativeUrl('{tgt}')", headers=h, timeout=30).status_code == 200: return
              requests.post(f"{api}/GetFolderByServerRelativeUrl('{par}')/folders/add(url='{quote(pn,safe='')}')", headers=h, timeout=30)

          def upload(token, path):
              sz   = os.path.getsize(path); fn = os.path.basename(path)
              print(f"📤 Uploading {fn} ({sz/1024/1024:.1f} MB)")
              h    = {'Authorization': f'Bearer {token}', 'Accept': 'application/json;odata=nometadata'}
              frel = f"{SITE_PATH}/{DOC_LIB}/{FOLDER_SUFFIX}"
              api  = f"https://{SITE_HOST}{SITE_PATH}/_api/web"
              furl = f"{api}/GetFolderByServerRelativeUrl('{frel}')"
              if sz <= 250*1024*1024:
                  with open(path,'rb') as f:
                      r = requests.post(f"{furl}/Files/add(url='{fn}',overwrite=true)",
                                        headers={**h,'Content-Type':'application/octet-stream'}, data=f, timeout=600)
                  if r.status_code not in [200,201]: print(f"❌ [{r.status_code}]: {r.text[:200]}"); sys.exit(1)
              else:
                  uid = str(uuid.uuid4())
                  from urllib.parse import quote as urlquote
                  file_srvrel = urlquote(f"{frel}/{fn}", safe='/:@!$&\'()*+,;=')
                  with open(path,'rb') as f:
                      offset = 0
                      while True:
                          chunk = f.read(CHUNK_SIZE)
                          if not chunk: break
                          first = offset == 0; last = offset+len(chunk) >= sz
                          if first:   url = f"{furl}/Files/add(url='{fn}',overwrite=true)/startUpload(uploadId=guid'{uid}')"
                          elif last:  url = f"{api}/GetFileByServerRelativeUrl('{file_srvrel}')/finishUpload(uploadId=guid'{uid}',fileOffset={offset})"
                          else:       url = f"{api}/GetFileByServerRelativeUrl('{file_srvrel}')/continueUpload(uploadId=guid'{uid}',fileOffset={offset})"
                          r = requests.post(url, headers={**h,'Content-Type':'application/octet-stream'}, data=chunk, timeout=300)
                          if r.status_code not in ([200,201] if last else [200,201,202]):
                              print(f"❌ Chunk [{r.status_code}]: {r.text[:200]}"); sys.exit(1)
                          offset += len(chunk)
              print(f"✅ Uploaded: {fn}")

          t = get_token(); ensure_folder(t); upload(t, ARTIFACT_FILE); print("✅ Done!")
          EOFPY
          python3 /tmp/upload_sp.py

      - name: Create deployment metadata
        run: |
          cat > deployment-metadata.json << EOF
          {
            "artifact": "${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz",
            "git_commit": "${{ github.sha }}",
            "git_commit_short": "${{ steps.vars.outputs.GIT_SHA_SHORT }}",
            "git_branch": "${{ github.ref_name }}",
            "git_author": "${{ github.actor }}",
            "build_timestamp": "${{ steps.vars.outputs.TIMESTAMP }}",
            "github_run_id": "${{ github.run_id }}",
            "github_run_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }
          EOF

      - name: Upload deployment metadata
        uses: actions/upload-artifact@v4
        with:
          name: deployment-metadata
          path: deployment-metadata.json
          retention-days: 7

      - name: Summary
        run: |
          echo "## 📦 Build PROD — app.formarte.co" >> $GITHUB_STEP_SUMMARY
          echo "| Campo | Valor |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Artifact** | \`${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Branch** | \`master\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Target** | app.formarte.co |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ steps.vars.outputs.GIT_SHA_SHORT }}\` |" >> $GITHUB_STEP_SUMMARY

  # ========== JOB 3: Build DEV (dev → dev-mongo.plataformapodium.com) ==========
  build-dev:
    name: Build DEV Docker Image (dev-mongo.plataformapodium.com)
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref_name == 'dev'

    env:
      PROJECT_NAME: dev-mongo.plataformapodium.com
      DOCKER_IMAGE_NAME: dev-mongo-plataformapodium

    outputs:
      artifact-name: ${{ steps.vars.outputs.ARTIFACT_NAME }}
      git-sha: ${{ steps.vars.outputs.GIT_SHA_SHORT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate build variables
        id: vars
        run: |
          GIT_SHA_SHORT=$(echo ${{ github.sha }} | cut -c1-7)
          TIMESTAMP=$(date +%Y%m%d-%H%M%S)
          BRANCH_CLEAN=$(echo ${{ github.ref_name }} | tr '/' '-')
          ARTIFACT_NAME="${{ env.DOCKER_IMAGE_NAME }}-${BRANCH_CLEAN}-sha-${GIT_SHA_SHORT}-${TIMESTAMP}"
          echo "GIT_SHA_SHORT=${GIT_SHA_SHORT}" >> $GITHUB_OUTPUT
          echo "TIMESTAMP=${TIMESTAMP}" >> $GITHUB_OUTPUT
          echo "ARTIFACT_NAME=${ARTIFACT_NAME}" >> $GITHUB_OUTPUT
          echo "📦 Artifact: ${ARTIFACT_NAME}.tar.gz"

      - name: Build Docker image
        run: |
          docker build \
            --tag ${{ env.DOCKER_IMAGE_NAME }}:${{ steps.vars.outputs.GIT_SHA_SHORT }} \
            --tag ${{ env.DOCKER_IMAGE_NAME }}:latest \
            --label "git.commit=${{ github.sha }}" \
            --label "git.branch=${{ github.ref_name }}" \
            --label "build.timestamp=${{ steps.vars.outputs.TIMESTAMP }}" \
            --label "build.actor=${{ github.actor }}" \
            .

      - name: Save & compress Docker image
        run: |
          docker save ${{ env.DOCKER_IMAGE_NAME }}:${{ steps.vars.outputs.GIT_SHA_SHORT }} \
            -o ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar
          echo "📊 Raw size: $(du -sh ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar | cut -f1)"
          gzip -9 ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar
          echo "📊 Compressed: $(du -sh ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz | cut -f1)"

      - name: Install Python dependencies
        run: pip install requests PyJWT cryptography

      - name: Upload artifact to SharePoint
        env:
          ARTIFACT_FILE: ${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CERT_THUMBPRINT: ${{ secrets.AZURE_CERT_THUMBPRINT }}
          AZURE_PRIVATE_KEY: ${{ secrets.AZURE_PRIVATE_KEY }}
          SHAREPOINT_SITE_URL: ${{ secrets.SHAREPOINT_SITE_URL }}
          SHAREPOINT_FOLDER: ${{ secrets.SHAREPOINT_FOLDER }}
        run: |
          cat > /tmp/upload_sp.py << 'EOFPY'
          import os, sys, time, uuid, hashlib, requests, jwt, base64, binascii
          from cryptography.hazmat.primitives import serialization
          from cryptography.hazmat.backends import default_backend

          TENANT_ID      = os.environ['AZURE_TENANT_ID']
          CLIENT_ID      = os.environ['AZURE_CLIENT_ID']
          THUMBPRINT     = os.environ['AZURE_CERT_THUMBPRINT']
          PK_PEM         = os.environ['AZURE_PRIVATE_KEY']
          SITE_URL       = os.environ['SHAREPOINT_SITE_URL'].rstrip('/')
          FOLDER_SUFFIX  = f"{os.environ['SHAREPOINT_FOLDER']}/{os.environ.get('PROJECT_NAME','')}"
          ARTIFACT_FILE  = os.environ['ARTIFACT_FILE']
          SITE_HOST      = SITE_URL.split('/')[2]
          SITE_PATH      = '/' + '/'.join(SITE_URL.split('/')[3:])
          DOC_LIB        = 'Documentos compartidos'
          CHUNK_SIZE     = 10 * 1024 * 1024

          def normalize_pem(s):
              return '\n'.join(l.strip() for l in s.strip().splitlines())

          def get_token():
              now = int(time.time())
              payload = {'aud': f'https://login.microsoftonline.com/{TENANT_ID}/oauth2/v2.0/token',
                         'exp': now+600, 'iss': CLIENT_ID,
                         'jti': hashlib.sha256(os.urandom(32)).hexdigest(),
                         'nbf': now, 'sub': CLIENT_ID}
              tb  = binascii.unhexlify(THUMBPRINT.replace(':','').replace(' ',''))
              x5t = base64.urlsafe_b64encode(tb).rstrip(b'=').decode('ascii')
              pk  = serialization.load_pem_private_key(normalize_pem(PK_PEM).encode(), password=None, backend=default_backend())
              ca  = jwt.encode(payload, pk, algorithm='RS256', headers={'x5t': x5t})
              r   = requests.post(f'https://login.microsoftonline.com/{TENANT_ID}/oauth2/v2.0/token',
                                  data={'client_id': CLIENT_ID,
                                        'client_assertion_type': 'urn:ietf:params:oauth:client-assertion-type:jwt-bearer',
                                        'client_assertion': ca,
                                        'scope': f'https://{SITE_HOST}/.default',
                                        'grant_type': 'client_credentials'}, timeout=30)
              if r.status_code != 200: print(f"❌ Token [{r.status_code}]: {r.text[:200]}"); sys.exit(1)
              return r.json()['access_token']

          def ensure_folder(token):
              from urllib.parse import quote
              api = f"https://{SITE_HOST}{SITE_PATH}/_api/web"
              h   = {'Authorization': f'Bearer {token}', 'Accept': 'application/json;odata=nometadata'}
              sp  = os.environ['SHAREPOINT_FOLDER']; pn = os.environ.get('PROJECT_NAME','')
              par = f"{SITE_PATH}/{DOC_LIB}/{sp}"; tgt = f"{par}/{pn}"
              if requests.get(f"{api}/GetFolderByServerRelativeUrl('{tgt}')", headers=h, timeout=30).status_code == 200: return
              requests.post(f"{api}/GetFolderByServerRelativeUrl('{par}')/folders/add(url='{quote(pn,safe='')}')", headers=h, timeout=30)

          def upload(token, path):
              sz   = os.path.getsize(path); fn = os.path.basename(path)
              print(f"📤 Uploading {fn} ({sz/1024/1024:.1f} MB)")
              h    = {'Authorization': f'Bearer {token}', 'Accept': 'application/json;odata=nometadata'}
              frel = f"{SITE_PATH}/{DOC_LIB}/{FOLDER_SUFFIX}"
              api  = f"https://{SITE_HOST}{SITE_PATH}/_api/web"
              furl = f"{api}/GetFolderByServerRelativeUrl('{frel}')"
              if sz <= 250*1024*1024:
                  with open(path,'rb') as f:
                      r = requests.post(f"{furl}/Files/add(url='{fn}',overwrite=true)",
                                        headers={**h,'Content-Type':'application/octet-stream'}, data=f, timeout=600)
                  if r.status_code not in [200,201]: print(f"❌ [{r.status_code}]: {r.text[:200]}"); sys.exit(1)
              else:
                  uid = str(uuid.uuid4())
                  from urllib.parse import quote as urlquote
                  file_srvrel = urlquote(f"{frel}/{fn}", safe='/:@!$&\'()*+,;=')
                  with open(path,'rb') as f:
                      offset = 0
                      while True:
                          chunk = f.read(CHUNK_SIZE)
                          if not chunk: break
                          first = offset == 0; last = offset+len(chunk) >= sz
                          if first:   url = f"{furl}/Files/add(url='{fn}',overwrite=true)/startUpload(uploadId=guid'{uid}')"
                          elif last:  url = f"{api}/GetFileByServerRelativeUrl('{file_srvrel}')/finishUpload(uploadId=guid'{uid}',fileOffset={offset})"
                          else:       url = f"{api}/GetFileByServerRelativeUrl('{file_srvrel}')/continueUpload(uploadId=guid'{uid}',fileOffset={offset})"
                          r = requests.post(url, headers={**h,'Content-Type':'application/octet-stream'}, data=chunk, timeout=300)
                          if r.status_code not in ([200,201] if last else [200,201,202]):
                              print(f"❌ Chunk [{r.status_code}]: {r.text[:200]}"); sys.exit(1)
                          offset += len(chunk)
              print(f"✅ Uploaded: {fn}")

          t = get_token(); ensure_folder(t); upload(t, ARTIFACT_FILE); print("✅ Done!")
          EOFPY
          python3 /tmp/upload_sp.py

      - name: Create deployment metadata
        run: |
          cat > deployment-metadata.json << EOF
          {
            "artifact": "${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz",
            "git_commit": "${{ github.sha }}",
            "git_commit_short": "${{ steps.vars.outputs.GIT_SHA_SHORT }}",
            "git_branch": "${{ github.ref_name }}",
            "git_author": "${{ github.actor }}",
            "build_timestamp": "${{ steps.vars.outputs.TIMESTAMP }}",
            "github_run_id": "${{ github.run_id }}",
            "github_run_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }
          EOF

      - name: Upload deployment metadata
        uses: actions/upload-artifact@v4
        with:
          name: deployment-metadata
          path: deployment-metadata.json
          retention-days: 7

      - name: Summary
        run: |
          echo "## 📦 Build DEV — dev-mongo.plataformapodium.com" >> $GITHUB_STEP_SUMMARY
          echo "| Campo | Valor |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Artifact** | \`${{ steps.vars.outputs.ARTIFACT_NAME }}.tar.gz\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Branch** | \`dev\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Target** | dev-mongo.plataformapodium.com |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ steps.vars.outputs.GIT_SHA_SHORT }}\` |" >> $GITHUB_STEP_SUMMARY
